При выполнении работы найдены и исправлены следующие ошибки:

Файл index.html.

1. Опечатки в тексте:

В блоке .name опечатка: «Основныые Интрументы» → должно быть «Основные Инструменты».

В подзаголовке опечатка: «Тетирование WEB‑приложений» → должно быть «Тестирование WEB‑приложений».

В ссылке на командную строку опечатка: «Windos» → должно быть «Windows».

2.Ошибки разметки и синтаксиса:

Лишний закрывающий тег </p> после подзаголовка — создаёт некорректную структуру.

Некорректный URL в подключении шрифта: https://fonts.googleapis.com/... → пропущена буква s в https, должно быть https://....

3.Ошибка в кодировке в мета‑теге: charset="utf-8" → должно быть charset="UTF-8".

Атрибут align="center" в теге <p> устарел. Лучше использовать CSS для выравнивания.

Тег <center> устарел — рекомендуется заменить на CSS‑стили.

В футере опечатка в годе: Лишний пробел в подвале страницы. Сейчас "202 4", а должно быть "2024".

4.Проблемы с путями к файлам:

Пути в ссылках (html/cmd.html, html/mac.html, html/git.html) могут быть некорректными в зависимости от структуры проекта. Рекомендуется проверить и при необходимости скорректировать.

Аналогично — пути в <link> и <img> (css/style.css, img/ava.png и т. д.) могут требовать уточнения относительно расположения файлов.

Прочие замечания:

Пустой блок <p> <br> </p> не несёт смысловой нагрузки и может быть удалён.

Отсутствие явной навигации на главную страницу на самой главной странице не является ошибкой, но стоит убедиться, что навигация есть на всех второстепенных страницах.

Файл cmd.html.

1. Опечатки и ошибки в разметке:

В теге <link rel="stylesheet"...> неверно указано значение атрибута: должно быть rel="stylesheet" → rel="stylesheet" (ошибка: rel="stylesheet" вместо rel="stylesheet").

Некорректный URL в подключении шрифта: https://fonts.googleapis.com/... → должно быть https://... (пропущена буква s в https).

Ошибка в кодировке в мета‑теге: charset="utf-8" → должно быть charset="UTF-8".

В футере опечатка в годе: Лишний пробел в подвале страницы. Сейчас "202 4", а должно быть "2024".

2.Отсутствие ссылки на главную страницу — на странице нет навигационной ссылки (кнопки/ссылки) на главную страницу портала.

3.Ошибки в описании команд Windows:

DIR — описание «Изменить каталог» неверно. На самом деле DIR отображает файлы и папки в текущем каталоге.

CD — описание «Отображение файлов и папок в текущем каталоге» неверно. Команда CD (Change Directory) используется для изменения каталога.

MAKEDIR — такой команды в стандартной командной строке Windows нет. Правильная команда для создания папки — MKDIR или MD.

DATE — описание «Выводит или устанавливает системное время» неверно. DATE работает с датой.

TIME — описание «Выводит или устанавливает текущую дату» неверно. TIME работает со временем.

Файл mac.html.

1. Опечатки в тексте:

В заголовке <title>Терминал MaOS</title> опечатка: MaOS → должно быть macOS.

В подзаголовке <p class="subtitle">Терминал MacOS</p> — Macos → должно быть macOS (правильное написание с маленькой буквы «c»).

В описании команды mkdir: опечатка «расфшифровывается» → должно быть «расшифровывается».

2.Ошибки в описании команд терминала macOS:

mv — неверно указано значение: «Make directory» («создать папку»). На самом деле mv расшифровывается как Move и используется для перемещения и переименования файлов/папок.

mkdir — неверно указано значение: «Move» («переместить»). Правильная расшифровка — Make directory, команда создаёт папки.

В описании mkdir ошибка в функционале: сказано, что можно «удалить файл». Команда mkdir не удаляет файлы — она только создаёт директории.

3.Ошибки разметки и синтаксиса:

Некорректный URL в подключении шрифта: https://fonts.googleapis.com/... → пропущена буква s в https, должно быть https://....

4.Ошибка в кодировке в мета‑теге: charset="utf-8" → должно быть charset="UTF-8".

Прочие замечания:

В описании команд используются кавычки разного стиля (иногда «ёлочки», иногда “лапки”). Для единообразия лучше использовать один тип.

Файл git.html

1. Опечатки в тексте:

В футере опечатка в тексте ссылки: «Вернуся на главную» → должно быть «Вернуться на главную».

В описании команды mv опечатка: «расфшифровывается» → должно быть «расшифровывается».

В футере опечатка в годе: Лишний пробел в подвале страницы. Сейчас "202 4", а должно быть "2024".

2.Отсутствие ссылки на главную страницу:

Ссылка в футере ведёт на mac.html (страницу про macOS), а не на главную (index.html). Это несоответствие названия и перехода.

3.Ошибки в описании команд Git:

Все перечисленные команды (ls, cd, pwd, mkdir, mv) не являются командами Git — это команды терминала/командной строки.

Описание команды mv содержит ошибку: утверждение, что она может «удалить файл» — неверно. Команда mv перемещает или переименовывает файлы/папки, но не удаляет их.

4.Ошибки разметки и синтаксиса:

Некорректный URL в подключении шрифта: https://fonts.googleapis.com/... → пропущена буква s в https, должно быть https://....

Ошибка в кодировке в мета‑теге: charset="utf-8" → должно быть charset="UTF-8".

Атрибут align="center" в теге <p> устарел. Лучше использовать CSS для выравнивания.

исправленный вид:

<!DOCTYPE html>
<html>
   <head>
      <title>Git — всему голова!</title>
      <link rel="stylesheet" type="text/css" href="../css/style.css">
      <link rel="stylesheet" type="text/css" href="../css/font-awesome.css">
      <link rel="shortcut icon" href="../img/fav.ico" type="image/x-icon">
      <link href="https://fonts.googleapis.com/css?family=Montserrat:700" rel="stylesheet">
      <meta charset="UTF-8">
      <meta name="viewport" content="width=device-width, initial-scale=1">
   </head>
   <body>
      <!-- Navigation -->
      <nav>
         <a href="../index.html">← На главную</a>
      </nav>

      <!-- Card -->
      <div class="card">
         <p class="subtitle">Git</p>
         <ul>
            <li>git init — инициализирует новый Git‑репозиторий в текущей папке.</li>
            <li>git clone [URL] — клонирует удалённый репозиторий на локальный компьютер.</li>
            <li>git status — показывает текущее состояние репозитория (изменённые файлы, staged‑файлы и т. д.).</li>
            <li>git add [file] — добавляет файлы в индекс (staging area) для следующего коммита.</li>
            <li>git commit -m "Сообщение" — фиксирует изменения в репозитории с указанным сообщением.</li>
            <li>git push — отправляет локальные коммиты в удалённый репозиторий.</li>
            <li>git pull — получает последние изменения из удалённого репозитория и объединяет их с локальными.</li>
            <li>git log — показывает историю коммитов.</li>
         </ul>
      </div>

      <!-- Footer -->
      <footer class="animated fadeIn">
         <p class="location" style="text-align: center;">
            <a href="../index.html">Вернуться на главную</a>
         </p>
         &copy; 2024. SkyPro
      </footer>
   </body>
</html>
Что было исправлено:

Исправлены опечатки:

«Вернуся» → «Вернуться»;

«расфшифровывается» → «расшифровывается»;

202 4 → 2024.

Исправлена навигация:

Ссылка в футере теперь ведёт на главную страницу (../index.html), а не на страницу про macOS.

Добавлена навигационная панель в начале страницы с ссылкой на главную.

Заменены команды:

Вместо команд терминала добавлены базовые команды Git с корректными описаниями.

Удалено ошибочное утверждение о возможности удаления файлов командой mv.

Исправлены ошибки в разметке:

URL шрифта: добавлена буква s в https;

кодировка: utf-8 → UTF-8;

устаревший атрибут align="center" заменён на встроенный CSS‑стиль style="text-align: center;".

Обновлён заголовок страницы: сохранён стиль, но исправлена пунктуация («Git - всему голова!» → «Git — всему голова!»).
